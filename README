这是一个简单的贪吃蛇游戏,最简单的黑白版本没有任何其他功能的版本.
      W     
      ^
A<-   |   ->D
      V
      S
AWSD控制蛇的上下左右移动.该游戏使用了ncurses库.



之前的代码，就有点脱裤子放屁了。因为好多功能，ncurses都已经提供了。
没必要再写多余的代码。使用一个产品，最重要的还是要看看产品说明书。
之后，基本上要大幅度的精简代码了。

25.4.12,今天刮大风。本来只是想吧游戏角色的结构通过复合链表。封装起来,
使程序更加容易理解。后来却发现问题越来越复杂。因为角色是在程序运行期间
加载的。所以需要在加载角色节点的时候判断角色是否有加载条件，应该在哪里
加载。对已加载的角色有什么影响。有这样能力的链表是不是可以作为一个通用
的角色控制库。可以使用在不同的游戏中呢？有没有必要把游戏地图，和显示屏
幕也都作为一个角色来对待呢？

设想一下,整个游戏的流程，设定好游戏的每一个元素后。有戏只需要根据元素的
运行逻辑。画好每一个元素。显示在游戏屏幕上。就可以了。游戏的运行逻辑，
和显示逻辑是分开的。

这回是彻底重写了整个游戏。使用了链表来管理游戏角色。

代码已经实现了主节点和子节点的统一结构，并且遍历、添加、删除等操作都采用
了递归和回调的方式。
## 1. 结构体设计
统一为一个 `ROLE_NODE` 结构体，包含 `next`、`prev`、`child_head`、
`parent` 指针，支持双向链表和多级子链表。
子节点链表通过 `child_head` 挂在父节点下，结构更清晰，代码更简洁。
## 2. 遍历的方式
只用一个 `traverseNodes` 递归函数，既能遍历主链表，也能递归遍历所有子链表。
支持回调机制，遍历时可灵活执行不同操作（如绘制、逻辑处理、碰撞检测、销毁等）。
## 3. 回调与功能分离
通过回调函数（如 `drawRoleCallback`、`logicCallback`、
`CollisionCallback`、`destroyRoleByIDCallback`），只需实现一次遍历
逻辑，具体操作通过回调实现，极大提升了代码复用性和可维护性。
## 4. 节点添加与删除的优化
添加子节点时直接插入到父节点的 `child_head` 链表，逻辑统一。
删除节点时，统一处理 `prev`、`next`、`head`、`tail` 指针，保证链表结构完整。
## 5. 递归与树形结构支持
递归遍历和递归删除支持任意层级的父子关系，天然支持树形结构，扩展性强。
## 6. 代码可读性和维护性提升
统一的遍历和回调机制让代码逻辑一目了然，便于后续扩展和维护。
## 总结
代码为单结构、递归遍历、回调分离，实现了
**结构统一、逻辑简洁、功能灵活、易于维护和扩展**的目标。
这是面向对象思想在C语言中的一种实践。


这样在以后添加新角色的时候，只需要添加一个新的节点就可以了。基本上可以不
用过多的关注功能的实现，可以专注于游戏的设计。
下一步是把游戏的显示逻辑和游戏的运行逻辑分开。这样就可以增加游戏的可移植
性。通过使用不同的图形库来实现不同的显示效果。比如使用SDL，SFML，或者直
接使用OpenGL来实现游戏的显示效果。
再下来可以考虑多人游戏的实现。使用网络库来实现多人游戏的功能。这样就可以实现
多人游戏的功能了。比如使用Boost.Asio，或者直接使用C++11的网络库来实现多
人游戏的功能。
